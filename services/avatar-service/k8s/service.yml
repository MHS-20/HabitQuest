apiVersion: v1
kind: Service
metadata:
  name: avatar-service
  labels:
    app: avatar-service
spec:
  type: ClusterIP
  selector:
    app: avatar-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9003
