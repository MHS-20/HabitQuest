apiVersion: v1
kind: Service
metadata:
  name: marketplace-service
  labels:
    app: marketplace-service
spec:
  type: ClusterIP
  selector:
    app: marketplace-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
