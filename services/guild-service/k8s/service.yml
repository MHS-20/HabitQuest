apiVersion: v1
kind: Service
metadata:
  name: guild-service
  labels:
    app: guild-service
spec:
  type: ClusterIP
  selector:
    app: guild-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
