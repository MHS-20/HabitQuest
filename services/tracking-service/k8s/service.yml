apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  labels:
    app: tracking-service
spec:
  type: ClusterIP
  selector:
    app: tracking-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
