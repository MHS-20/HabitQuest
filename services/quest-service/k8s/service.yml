apiVersion: v1
kind: Service
metadata:
  name: quest-service
  labels:
    app: quest-service
spec:
  type: ClusterIP
  selector:
    app: quest-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9003
