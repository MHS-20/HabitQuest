loki:
  serviceName: loki
  servicePort: 3100
  serviceScheme: http

config:
  outputs: |
    [OUTPUT]
        Name        loki
        Match       *
        Host        loki.logging.svc.cluster.local
        Port        3100
        Labels      job=fluent-bit
